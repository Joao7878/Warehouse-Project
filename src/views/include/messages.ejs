<% if (success.length >0) { %>

<div class="success">
    <% success.forEach(suc => { %>
    <%= suc %> <br>
    <% }); %>
</div>
<% } %>

<% if (errors.length >0) { %>
<div class="errors">
    <% errors.forEach(err => { %>
    <%= err %> <br>
    <% }); %>
</div>
<% } %>